Refactor caf-vec
 -> Clean up include directives in caf-vec.cpp
 -> rename caf-vec.cpp to main.cpp
 -> Add namespacing
 -> General clean up
    -> Make things be member functions for instance if it makes sense.
 -> Add doxygen
 -> Check coding_guidelines.txt

Try to set up sourcetrail for caf-vec

Figure out why the log produced by caf-vec ends up so
that ShiViz won't accept it on the laptop.

